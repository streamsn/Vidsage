export default function CheckoutSuccessPage() {
  return (
    <main className="mx-auto flex max-w-lg flex-col items-center gap-4 px-6 py-24 text-center">
      <h1 className="text-2xl font-semibold">Thanks — credits are on the way</h1>
      <p className="text-slate-600">
        Head back to the extension side panel; your balance updates automatically.
      </p>
    </main>
  );
}

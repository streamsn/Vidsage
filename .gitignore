node_modules/
.next/
dist/
build/
.env
.env.local
*.local
.DS_Store
.vercel
.turbo
*.tsbuildinfo

# Extension signing key: pins the Chrome extension ID (see apps/extension/EXTENSION_ID).
# The private half must never be committed or shared.
apps/extension/keys/
